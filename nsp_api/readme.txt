* Encrypting sensitive values with Jasypt:
-----------------------------------------------

Visit link https://www.devglan.com/online-tools/jasypt-online-encryption-decryption to encrypt the values and place them in config files when and where required.

Example: DB passwords

Example to place the encrypted value in config files, ex: ENC(encrypted-value)